# Environment Variables Template
# 이 파일을 복사하여 .env.development 또는 .env.production으로 사용하세요

# Environment
NODE_ENV=development
PORT=3002

# Database
# Development: Docker Compose 사용 시
# DATABASE_URL=postgresql://consultation_user:consultation_pass@localhost:5433/consultation_booking_dev
# Production: AWS RDS 또는 외부 PostgreSQL
# DATABASE_URL=postgresql://user:password@host:5432/database

DATABASE_URL=postgresql://user:password@host:port/database

# Google OAuth
# Google Cloud Console에서 발급받은 Client ID와 Secret
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# JWT
# 프로덕션에서는 강력한 랜덤 문자열 사용 (최소 32자)
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters
JWT_EXPIRES_IN=7d

# Frontend URL (CORS 설정용)
# Production에서만 필요
FRONTEND_URL=http://localhost:5173